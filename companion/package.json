{
  "name": "touchdeck-companion",
  "version": "0.2.0",
  "private": true,
  "description": "TouchDeck macOS menu-bar companion — launches apps from BLE tile_press events",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer dist/web-install && cp -R ../web/install/. dist/web-install/ && cp src/renderer/index.html src/renderer/app.js dist/renderer/",
    "start": "pnpm run build && electron .",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "electron": "^33.2.0",
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron"]
  }
}
